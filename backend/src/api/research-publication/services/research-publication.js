'use strict';

/**
 * research-publication service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::research-publication.research-publication');
