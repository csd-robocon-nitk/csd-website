'use strict';

/**
 * research-publication router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::research-publication.research-publication');
