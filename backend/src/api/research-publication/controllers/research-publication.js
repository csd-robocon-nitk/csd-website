'use strict';

/**
 * research-publication controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::research-publication.research-publication');
